import {test, expect} from '@playwright/test';

test.describe('Test for AllowEditInvariantFromNonDefault=False', () => {
  const baseUrl = 'http://localhost:44331';

  test('Can create language', async ({request}) => {

  });
});
